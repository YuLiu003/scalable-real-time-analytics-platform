apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-data
  namespace: analytics-platform
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 10Gi 